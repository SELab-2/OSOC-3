import styled from "styled-components";

export const SearchInput = styled.input.attrs({
    placeholder: "Search",
})`
    width: 150px;
    font-size: 15px;
    border-radius: 5px;
    border: none;
    height: 28px;
`;
