import styled from "styled-components";

export const SearchInput = styled.input.attrs({
    placeholder: "Search",
})`
    margin: 3px;
    width: 150px;
    font-size: 15px;
    border-radius: 5px;
    border-width: 0;
`;
