import styled from "styled-components";

export const StudentRemoveButton = styled.button`
    position: absolute;
    width: 150px;
    height: 35px;
    right: 5%;
    top: 12%;
    color: white;
    background-color: var(--osoc_red);
    border: 4px solid var(--osoc_red);
`;

export const StudentInfoPageContent = styled.div`
    display: flex;
`;
