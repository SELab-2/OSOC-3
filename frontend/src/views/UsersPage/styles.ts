import styled from "styled-components";
import { Button } from "react-bootstrap";

export const UsersPageDiv = styled.div``;

export const AdminsButton = styled(Button)`
    background-color: var(--osoc_green);
    margin-right: 10px;
    margin-top: 10px;
    float: right;
`;

export const UsersHeader = styled.div`
    padding-left: 10px;
    margin-top: 10px;
    float: left;
    display: inline-block;
`;
